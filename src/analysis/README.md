# src/analysis/ — 지표를 조합해 판단을 만드는 단계

## 왜 필요했나

`indicators/`가 만드는 것은 **지표**다. 표면주거비가 얼마고 생활소비부담지수가 몇인지까지가 그쪽 일이다.

이 폴더가 하는 것은 **판단**이다. 그 지표들을 합쳐서 "이 동은 월세는 싸 보이지만 통근까지 넣으면 그렇지 않다", "이 동네들은 같은 성격으로 묶인다" 같은 결론을 만든다.

둘을 나눈 이유는 **실행 주기가 다르기** 때문이다. 지표 산출은 원본 데이터가 갱신될 때만 돌면 된다. 반면 분석은 기준을 바꿔가며 여러 번 돌려보는 작업이다. 시간가치를 바꾸거나 군집 수를 바꿀 때마다 전처리부터 다시 돌 이유가 없다.

여기에 트랙이 하나 더 붙었다. **지표가 얼마나 믿을 만한지를 재는 일**이다. 서비스가 붙으면서 "이 숫자를 사용자에게 그대로 보여줘도 되는가"에 답해야 했다.

## 실행 순서

### 분석 트랙

세 파일은 **반드시 이 순서**로 돈다. 뒤 단계가 앞 단계 산출물을 읽는다.

```
define_business_districts.py   업무지구 판정
        ↓
build_total_burden.py          통합부담 테이블 (지표 전부 결합)
        ↓
dong_typology_final            행정동 6유형 군집 (팀원A, FuzzyCMeans)
```

앞선 단계로는 `reference/build_region_master.py`(행정동 기준표 427행)가 있어야 하고, 뒤로는 `validation/validate_cluster_quality.py`가 군집 결과를 진단한다.

### 신뢰도 트랙

분석 트랙과 독립적으로 돈다. 서비스에 붙일 등급을 만든다.

```
verify_road_accuracy.py        도로명 배정 정확도 검증
        ↓
build_dong_reliability.py      판정 4종을 합쳐 신뢰도 등급
extract_adjacent_by_bjd.py     결측 동 인근 후보 (독립 실행)
```

---

## define_business_districts.py

### 문제였던 점

"업무지구가 어디인가"는 생각보다 답하기 어렵다. 한 축만 보면 반드시 오판한다.

- **규모만 보면** 인구가 많은 주거지가 섞여 들어온다
- **주야간 인구비만 보면** 규모가 작은 상업지가 상위에 올라온다

그래서 판정을 3축으로 나눴다.

| 축 | 기준 | 막는 오판 |
|---|---|---|
| 규모 | 출근 유입량 상위 15% | 작은 상업지 |
| 방향 | 유입/유출 비율 > 1 | 베드타운 |
| 시간 | 주야간 인구비 > 1 | 야간 인구 많은 곳 |

유입량을 계산할 때 **내부통근은 뺀다.** 넣으면 유입과 유출이 동시에 부풀려져 비율이 1에 가까워지고, 방향 축이 무력해진다.

### 하는 일

판정과 지구 구성을 **분리한다.** 판정은 데이터가 하고, "업무지구로 부를 것인가"는 사람이 판단한다. 제외한 15개는 사유를 코드에 남겼다 — 압구정·청담·신사는 상업/유흥, 신촌·충현은 대학가, 서교동은 홍대 상권, 양재는 R&D 별도 통근권, 공항동은 공항 특수, 풍납2동은 병원, 양평제2동은 준공업 제조·물류다.

지구 안의 행정동마다 `지구내_가중치`(유입량 비중)를 부여한다. 지구 대표 통근시간·교통비를 낼 때 가중평균에 쓴다.

**결과** 판정 55개(전체 유입의 52.6% 흡수), 최종 9개 지구 44.1%
강남 14.76 · 도심 12.01 · 여의도영등포 6.08 · 구로가산 3.67 · 송파문정 2.39 · 성수 1.55 · 용산 1.39 · 마곡강서 1.33 · 상암 0.88

**입력** `행정동_기준코드표.csv`, `commute_burden_with_network_metrics.csv`, 생활인구 지표
**출력** `업무지구_정의.csv`(40행), `업무중심성_행정동별.csv`(427행)

---

## build_total_burden.py

### 문제였던 점

주거비만 비교하면 통근이 빠진다. 그런데 통근을 넣으려면 단위를 맞춰야 한다. 시간은 원이 아니기 때문이다.

**통합부담 = 표면주거비 + 월 교통비 + 월 시간비용**

시간비용은 월 통근시간 × 시간가치(최저임금 10,320원)로 환산한다. 실제로 통장에서 빠져나가는 돈이 아니므로, 산출 파일에서 실지출과 분리해 병기한다.

지수 결합은 **코드 우선·이름 fallback**이다. 이름으로 붙이면 표기 차이에서 조용히 실패한다.

### 하는 일

지표를 전부 모아 행정동 하나당 한 행으로 만든다. 이 파일이 이후 모든 분석의 마스터 테이블이 된다.

**핵심 발견 — 교통비 격차는 정기권으로 소멸한다.**

월 교통비 실지출 격차는 58,345원인데, 기후동행카드 62,000원 캡을 적용하면 3,284원으로 줄어든다(428개 중 407개가 캡 대상). 항목별 격차를 세우면 이렇다.

| 항목 | 격차 |
|---|---|
| 주거비 | 981,333원 |
| 시간비용 | 189,733원 |
| 교통비(정기권) | 3,284원 |

**상쇄는 교통비가 아니라 시간비용에서 일어난다.** 싼 동네로 가면 교통비가 더 든다는 통념이 서울에서는 성립하지 않는다.

주거비 순위와 통합부담 순위의 스피어만은 0.958이지만, 50계단 이상 움직인 동이 63개(15.0%)다. 이 괴리로 부담유형을 넷으로 나눈다.

| 유형 | 개수 | 대표 |
|---|---|---|
| A 실질 저부담 | 197 | — |
| B 월세착시 | 13 | 고덕제2동(-119), 도봉제2동(-90) |
| C 숨은효율 | 13 | 이화동(+105), 종로5.6가동(+83) |
| D 종합 고부담 | 197 | — |

### 읽을 때 주의

`대표_편도통근시간_분`은 **거주동 거주자의 모든 목적지를 가중평균한 값**이다. 특정 근무지까지의 시간이 아니다. 근무지 기준 값이 필요하면 `fact_commute_route`(거주동 × 근무동)를 써야 한다.

**입력** 표면주거비, 생활소비부담지수, `commute_burden_with_network_metrics.csv`, 업무중심성
**출력** `주거통근_통합부담_행정동별.csv`(420행 43컬럼)

---

## cluster_dong_types.py → dong_typology_final (k=6)

> **2026-08-13 모델 교체.** 기존 `cluster_dong_types.py`(KMeans k=7)는 폐기했다.

### 왜 필요한가

420개 동을 하나씩 볼 수는 없다. **비슷한 성격끼리 묶어서** 유형으로 이해해야 한다.

### 입력 변수 5개 — 블록 균형 50:50

| 블록 | 변수 |
|---|---|
| 부담 50% | 표면주거비(거래단위 pooled 중앙값), 대표 편도통근시간, 월 통근교통비 |
| 구조 50% | 동일통근권 내부출근비율, 목적지 정규화 엔트로피 |

**블록 균형이 핵심이다.** 표준화만 하고 그냥 넣으면 변수가 많은 부담 계열이 자동으로 더 큰 영향을 갖는다. 부담과 구조를 각각 50%로 맞춰야 "얼마나 드는가"와 "어떻게 통근하는가"가 대등하게 반영된다.

### 설계 판단 4가지

**① 파생변수를 뺐다.**
시간기회비용은 통근시간에 상수를 곱한 값이고, 통합부담은 여러 변수의 합이다. 넣으면 **같은 정보를 두 번 센다.** 군집 형성이 아니라 **해석**에만 쓴다.

**② 청년1인세대비율과 생활소비부담지수를 군집에서 뺐다.**
둘 다 **원인이 아니라 결과**다. 청년이 많아서 부담구조가 그런 게 아니라, 부담구조가 그래서 청년이 모인 것이다. 넣으면 "청년이 많으니 청년밀집형"이라는 동어반복이 된다.

빼고 나서 **사후 검정**하면 훨씬 강한 주장이 된다 — 군집에 안 쓴 변수에서 유형별 차이가 유의하다면, 그것이 유형화가 실재를 잡았다는 증거다. Kruskal-Wallis H=65.85, p<0.001.

생활소비부담지수를 뺀 사유는 **"건당 결제금액이 가격 수준인지 소비 수준인지 분리할 수 없다"**는 것이다. (상권 규모는 건당 금액으로 나눠서, 업종 구성은 업종별 z-score로 이미 통제돼 있으므로 그것이 사유가 아니다.)

**③ 목적지 집중도는 HHI 대신 정규화 엔트로피를 쓴다.**

| | HHI | 정규화 엔트로피 |
|---|---|---|
| 내부통근비중과 상관 | **0.873** | 0.66 |
| 분포 | 중앙 0.020, 최대 0.208 (치우침) | 0.53~0.86 (고름) |

HHI는 사실상 **"자기 동에서 일한다"를 재고 있었다.** 다른 변수와 겹치는 지표는 쓸모가 없다.

**④ 결측은 대체하지 않고 남긴다.**
표면주거비가 없는 7개 동(하계2·상계8·상계9·반포본·반포2·오륜·잠실7)은 kNN으로 채우지 않는다. 전부 **대단지 아파트 지역이라 비아파트 임차 거래가 아예 없는 곳**이다. 채우면 "이 동네에 청년 원룸이 있다면 이 정도일 것"이라는 가정이 되고, 서비스에서 추천 후보로 뜨면 사용자가 실제로 매물이 없어 헛걸음한다. 유형을 비우고 `유형화_데이터부족` 플래그를 붙인다.

이 판단은 서비스까지 이어진다. `extract_adjacent_by_bjd.py`가 만드는 인근 후보도 **값을 대체하지 않고 선택지만 제시한다.**

### 알고리즘과 k 선택

**FuzzyCMeans (m=1.5), k=6.** 알고리즘 15종 × 변수표현 4종 = 615개 후보를 비교해 선정했다. 서비스 제약(최소 군집 30개, 최대 38%)을 사전에 걸어 "이 유형에 3개 동밖에 없어요"가 나오지 않게 했다.

퍼지 방식이라 **소속도(`최대소속확률`)가 부산물로 나온다.** 63개 동(15%)이 경계에 걸쳐 있고, 이건 버릴 정보가 아니라 서비스에서 "이 동은 두 유형 성격을 함께 가집니다"라고 표시할 근거가 된다.

### 6개 유형

| 유형 | 동수 | 주거비 | 통근 | 교통비 | 청년1인세대 | 소속확률 |
|---|---|---|---|---|---|---|
| 저주거비·지역연계형 | 115 | 58.8만 | 31.4분 | 66,618 | 16.9% | 0.703 |
| 근접통근 균형형 | 85 | 69.6만 | 25.7분 | 64,092 | 21.9% | 0.646 |
| 광역분산 통근형 | 72 | 63.5만 | 28.5분 | 65,650 | 22.4% | 0.788 |
| 주거절감·장거리통근형 | 66 | 56.1만 | **35.0분** | 67,766 | **12.6%** | **0.813** |
| 고비용 통근부담형 | 51 | 77.5만 | 30.1분 | **73,600** | 16.2% | 0.625 |
| 고주거비·직주근접형 | 31 | **86.3만** | **21.4분** | 67,755 | **27.9%** | 0.763 |
| (데이터 부족) | 7 | — | — | — | — | — |

**양 끝이 프로젝트의 결론이다.** 주거절감·장거리통근형(56.1만·35.0분)과 고주거비·직주근접형(86.3만·21.4분)은 정확히 반대편에 있고, 청년 비율이 12.6% vs 27.9%로 2배 이상 갈린다.

> **청년은 비싸도 가까운 곳에 산다.**

이 문장은 군집에 쓰지 않은 변수에서 나왔기 때문에 외적 타당성 근거가 된다.

경계 선명도는 **주거절감·장거리통근형이 0.813으로 가장 뚜렷하다.** 월세착시의 핵심 유형이 가장 선명하게 분리됐다는 뜻이라 프로젝트 메시지에 유리하다. 반대로 근접통근 균형형(0.646)과 고비용 통근부담형(0.625)은 경계가 흐릿하므로, 서비스에서 이 두 유형에 배정된 동은 유형을 단정하기보다 실제 수치를 보여주는 편이 낫다.

**입력** `주거통근_통합부담_행정동별.csv`, `표면주거비_거래단위.csv`, 청년1인세대 파일
**출력** `dong_typology_final.csv`(427행 22컬럼)

---

## verify_road_accuracy.py

### 문제였던 점

표면주거비 거래의 **46.6%가 도로명 근사**로 행정동에 배정됐다. `kakao_cache.json`의 키가 `시도|||자치구|||도로명` 형식이라 건물번호가 없고, 하나의 도로 전체에 행정동 하나를 복사해 쓴다. 단독다가구는 국토부가 지번을 `1*`, `2*`로 마스킹해 공개하므로 정밀 배정이 구조적으로 불가능하다.

이 한계는 `src/preprocessing/README.md`에 이미 기록돼 있었다. 그런데 **"위험이 남아있다"에서 멈춰 있었다.** 크기를 재지 않았다.

기존 QC로는 잡히지 않는다. QC6에서 파이프라인 값과 팀원 재집계가 만원 이상 차이 0으로 일치했지만, **둘 다 같은 캐시를 썼으므로 일치하는 것이 당연하다.** 정합성 검사는 "같은 방법을 두 번 써서 같은 답이 나오는가"를 볼 뿐이다.

### 하는 일

**정답을 아는 거래에 도로명 근사를 일부러 적용해 대조한다.**

검증 표본은 법정동이 행정동 하나에만 대응하고 캐시에 적중한 거래 5,821건이다. 정답이 캐시와 무관하게 결정되므로 검증에 쓸 수 있다.

| 구분 | 검증 n | 정확도 |
|---|---|---|
| 전체 | 5,821 | 62.4% |
| **길**(지선급) | 2,742 | **82.7%** |
| **로**(간선급) | 3,060 | **44.0%** |

간선도로가 여러 행정동을 관통한다는 가설이 그대로 확인됐다.

검증 표본과 실제 적용 대상은 도로 구성이 다르다. 도로명 근사가 실제로 적용되는 단독다가구 거래는 길 91.8% / 로 7.3%로 지선급에 쏠려 있어, 도로유형별 정확도를 모집단 구성비로 재가중한다.

**재가중 정확도 80.1%, 전체 오배정 추정 9.3%.**

다만 이것도 상한으로 봐야 한다. 검증 표본은 법정동이 쪼개지지 않은 지역, 즉 동 경계가 단순한 곳이다. 실제 적용 대상은 분동이 애매한 곳으로 조건이 더 어렵다.

### 검증하지 못한 것

**연립다세대·오피스텔이 도로명으로 폴백된 경우는 재현할 수 없었다.** 거래단위 CSV의 `도로명` 컬럼이 주택유형별로 형식이 다르다. 단독다가구는 순수 도로명(`마조로`)인데 연립다세대·오피스텔은 건물번호가 붙어 있다(`양천로 344`). 후자는 캐시 키를 만들 수 없다.

### 컬럼을 추측하지 않는다

이 스크립트는 2단계로 쓴다.

```
python src/analysis/verify_road_accuracy.py          # 점검. 스키마 덤프 + config 템플릿
python src/analysis/verify_road_accuracy.py --run    # 실행
```

점검 모드가 전체 컬럼을 고유값 수·샘플과 함께 출력한다. 그걸 보고 `docs/verify_config.json`을 채운 뒤 실행한다.

**정규식 자동 감지를 뺀 이유가 있다.** 부분문자열 첫 매칭 승리 때문에 세 번 틀렸다 — `행정동코드`(215개)가 `행정동코드_최종`(420개)보다 먼저 잡히고, `교통비_커버리지부족`(불리언)이 `교통비_산출포함률`(실수)보다 먼저 잡히고, `교통비`로 찾으니 `월교통비_실지출_원`이 먼저 잡혔다.

세 번째가 특히 나빴다. 불리언을 숫자로 읽어 전부 NaN이 되고, 결과가 **"교통비 커버리지 미달 0개"**로 나왔다. 실제로는 9개다. 틀린 값이 아니라 **"안전하다"는 거짓 신호**를 냈다.

**입력** `표면주거비_거래단위.csv`, `kakao_cache.json`, `대한민국 법정동 행정동 코드.xlsx`
**출력** `docs/도로명근사_정확도_동별.csv`

---

## build_dong_reliability.py

### 하는 일

흩어져 있는 판정 4종을 합쳐 행정동 하나당 등급 하나로 만든다.

```
결측 (커버리지 CSV)
정확도 (도로명근사_정확도_동별.csv)
교통비 커버리지 (통합부담 CSV)
거래 수 (배정방식_보정 CSV)
        ↓
   dim_dong_reliability  427행
```

| status | 개수 | 조건 |
|---|---|---|
| `ok` | 390 | — |
| `low_confidence` | 26 | 정확도 50% 미만 7 / 교통비 커버리지 70% 미만 9 / 거래 30건 미만 10 |
| `unreliable` | 4 | 검증 정확도 0%. 마장·성북·석관·구산동 |
| `no_data` | 7 | 표면주거비 미산출 |

`unreliable` 4개 동은 거래가 소수 간선도로에 몰려 있고 그 도로들의 캐시 대표점이 전부 이웃 동에 찍혔다. 성북동은 검증 거래 66건이 동소문로 하나에 있고 그 도로가 길음2동으로 잡힌다.

**임계값 방식을 한 번 바꿨다.** 처음에는 근사배정 비중으로 등급을 매기려 했는데, 중앙값이 59%라 어느 임계값을 잡아도 300개 넘는 동이 걸렸다. 구분의 의미가 없다. 그래서 전역 주석으로 한계를 밝히고, 개별 라벨은 극단 케이스에만 붙이는 쪽으로 설계를 바꿨다.

임계값은 상단 상수로 뺐다. 고치고 다시 돌리면 CSV와 SQL이 함께 갱신된다.

```
TH_UNRELIABLE_ACC = 0.001   검증 정확도 0%
TH_UNRELIABLE_N   = 50      그 판정에 필요한 최소 검증표본
TH_LOWCONF_ACC    = 0.50    정확도 50% 미만
TH_COVERAGE       = 0.70    교통비 산출포함률 하한
TH_SMALL_TX       = 30      소표본 기준
```

SQL을 직접 짜지 않고 스크립트가 생성하게 한 이유는 **427개 동 코드를 손으로 넣을 수 없기** 때문이다. 임계값 재조정이 쉬워지는 것은 부수 효과다.

**입력** `행정동_소스별_커버리지.csv`, `도로명근사_정확도_동별.csv`, `주거통근_통합부담_행정동별.csv`, `표면주거비_배정방식_동별_보정.csv`
**출력** `docs/행정동_신뢰도.csv`, `sql/dim_dong_reliability.sql`

---

## extract_adjacent_by_bjd.py

### 문제였던 점

표면주거비가 없는 7개 동을 사용자가 요청했을 때 인근 동을 제시하려면 **"인접"을 정의해야** 한다. 그런데 레포에 행정동 좌표가 없었다.

두 번 실패했다.

| 시도 | 결과 |
|---|---|
| 좌표 기반 직선거리 | CSV 62개에 위경도 컬럼 0개, GeoJSON·shp 없음 |
| 도로명 공유 관계 역산 | 캐시 키에 건물번호가 없어 도로당 행정동 1개뿐. 인접 그래프가 빈 상태 |

### 하는 일

**행정동은 법정동을 쪼개서 만들었으므로, 같은 법정동에 속한 행정동은 인접할 가능성이 높다.**

`대한민국 법정동 행정동 코드.xlsx`(KIKmix)에서 서울·유효 행을 걸러 746행을 얻는다. 고유 행정동코드 427개로 기준코드표와 전수 일치했다. 법정동 기준으로 그룹핑해 결측 동과 같은 법정동을 공유하는 행정동을 후보로 모은다.

좌표 거리보다 나은 점도 있다. **사용자 직관에 맞는다.** "같은 방이동 내 인근 행정동"이 "직선거리 1.2km"보다 설명하기 쉽다.

### 자동 추출로 끝내지 않는다

법정동 공유는 지리적 인접의 **충분조건이 아니다.** 자동 추출 24행을 행정동 경계도로 육안 검증해 16행으로 줄였다.

**중계4동이 그 사례다.** 법정동 상계동을 공유해 상계8·9동의 후보로 추출됐지만, 지도상 사이에 상계5동·상계2동·상계3,4동이 놓여 있어 맞닿지 않았다. 자동 추출만 믿었다면 상계8동을 요청한 사용자에게 중계4동이 제시됐을 것이다.

확정 결과와 근거는 [`docs/결측동_인근후보_확정근거.md`](../../docs/결측동_인근후보_확정근거.md).

**입력** `대한민국 법정동 행정동 코드.xlsx`, `행정동_소스별_커버리지.csv`
**출력** `docs/fallback_후보_법정동기반.csv`(24행 초안)

---

## 폐기 예정

시도했다가 실패한 것들이다. 결론은 문서에 남아 있으므로 정리해도 된다.

| 스크립트 | 폐기 사유 |
|---|---|
| `check_fallback_candidates.py` | 행정동 좌표 소스가 레포에 없음을 확인하고 종료 |
| `extract_adjacent_dongs.py` | 도로명 공유 방식. 캐시 구조상 인접 관계 추출 불가 |
| `aggregate_assign_method.py` | 컬럼 정규식 오매칭으로 결과가 전부 틀림 |

`cluster_dong_types.py`(KMeans k=7)도 폐기 상태다. 경위는 `docs/폐기_모델_기록.md`.

---

## 요약

| 파일 | 하는 일 | 산출물 |
|---|---|---|
| `define_business_districts.py` | 업무지구 3축 판정 | `업무지구_정의.csv` 40행, `업무중심성_행정동별.csv` 427행 |
| `build_total_burden.py` | 지표 결합 + 부담유형 ABCD | `주거통근_통합부담_행정동별.csv` 420행 |
| `dong_typology_final` | FuzzyCMeans k=6, 블록균형 50:50 | `dong_typology_final.csv` 427행 |
| `verify_road_accuracy.py` | 도로명 배정 정확도 역산 | `docs/도로명근사_정확도_동별.csv` |
| `build_dong_reliability.py` | 판정 4종 → 신뢰도 등급 | `docs/행정동_신뢰도.csv`, `sql/dim_dong_reliability.sql` |
| `extract_adjacent_by_bjd.py` | 법정동 공유 기반 인접 후보 | `docs/fallback_후보_법정동기반.csv` 24행 |

## 함께 볼 문서

| 문서 | 내용 |
|---|---|
| [`docs/폐기_모델_기록.md`](../../docs/폐기_모델_기록.md) | KMeans k=7 모델, 군집 품질 5축 진단, 교체 경위 |
| [`docs/표면주거비_배정_신뢰도.md`](../../docs/표면주거비_배정_신뢰도.md) | 오배정률 검증 방법과 수치, 검증 불가 영역 |
| [`docs/결측동_인근후보_확정근거.md`](../../docs/결측동_인근후보_확정근거.md) | 인근 후보 16행을 어떻게 정했나 |
| `src/validation/README_validation.md` | 지표 타당성 검증 |
| `src/db/README.md` | DB 적재와 조회 계층 |
| `sql/README.md` | 스키마와 조회 쿼리 |